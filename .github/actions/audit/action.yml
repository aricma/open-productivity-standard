name: OPS Audit
description: Scan the dependency tree for vulnerabilities and license policy.

runs:
  using: composite
  steps:
    - uses: rustsec/audit-check@v2
      with:
        token: ${{ github.token }}
    - uses: EmbarkStudios/cargo-deny-action@v2
